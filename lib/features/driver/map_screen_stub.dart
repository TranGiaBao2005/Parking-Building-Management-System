import 'package:flutter/widgets.dart';

/// Non-web stub — returns an empty placeholder.
/// The _FakeMapCanvas in map_screen.dart handles the actual rendering
/// when kIsWeb is false.
Widget buildWebMapView(double lat, double lng) {
  return const SizedBox.expand();
}
